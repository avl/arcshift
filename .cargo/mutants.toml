exclude_re = [
    ".*replace is_superseded_by_tentative -> bool with false.*", #Because it still works, it just goes a little slower with this change, so it doesn't fail the test
    "replace > with == in ArcShift<T>::reload" #Dead code, since we never test/achieve 35000000000000 instances.
]
